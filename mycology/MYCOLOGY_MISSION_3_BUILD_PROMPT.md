# Build Mycology Mission 3 Detail Checking

Confirm Missions 1–2 checkpoints and gates. Read the complete base, `missions/MISSION_03_DETAIL_CHECKING.md`, reviewed asset subset, and existing state immediately before coding. Implement Mission 3 only and safe migration. Copy the specification unchanged to `mycology/docs/MISSION_03_DETAIL_CHECKING.md`.

Render five fictional cases as live HTML with exactly one ID mismatch and one surname mismatch. Preserve exact decision/feedback copy, privacy rules, and accepted skin-case output. Test deterministic case construction, field comparison, correct/incorrect decisions, persistence repair, predecessor enforcement, and no hub award.

Run all tests and smoke Missions 1–2. Verify in a real browser every case decision, wrong retries/hints, refresh/Continue, keyboard/mouse/touch, reduced motion, accessible comparison structure, layouts, clean console/network/assets, and `git diff --check`.

Do not read or implement Mission 4 until the gate passes and this exact commit exists:

`Build Mycology Mission 3 detail-checking`
